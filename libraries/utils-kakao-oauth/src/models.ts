export interface KakaoToken {
  accessToken: string;
  refreshToken: string;
}

export interface KakaoOAuthContext {
  clientId: string;
  clientSecret: string;
}

export interface KakaoAccount {
  id: number;
}
