{
  "name": "@divops/axios",
  "version": "0.0.17",
  "main": "src/index.ts",
  "publishConfig": {
    "main": "dist/index.js"
  },
  "scripts": {
    "build": "rm -rf dist && divops-build",
    "deploy": "npm publish --access public"
  },
  "devDependencies": {
    "@divops/builder": "workspace:^",
    "@types/debug": "^4",
    "debug": "^4.3.3",
    "typescript": "^4.6.3"
  },
  "dependencies": {
    "axios": "^0.26.1"
  }
}
